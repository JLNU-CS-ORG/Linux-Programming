# Chapter6: Using curses library

If you want to compile files at current directory, you should add a options on command line.</br>
Please use `gcc *.c -lcurses` to compile. Because we used `ncruses` library.
